# discovered spec: doc flow — a behaviour lock; regenerate via this repo's freeze path and ratify the diff.

- submit undoes revise — the round trip is the identity.
      submit(revise(x)) = x

# operators in no law (where the spec is silent): approve, edit
