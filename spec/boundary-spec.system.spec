# system spec: boundary-spec — the seam graph (modules + seam obligations); regenerate via this repo's freeze path and ratify the diff.

modules (the ratified registry — one committed module lock each):
- interpreter arithmetic
- router
- date calculus
- ttl store
- store protocol

seams: none — no module pair declares a shared-value obligation.
