# GENERATED by genesis from the `relay-app` system declaration — the layout is DERIVED,
# not transcribed. Regenerate from the declaration rather than hand-porting structure.
#
# THE TRANSFORM-SEAM DEMO (examples/genesis_relay.rs is the seed): generated, then ONLY the
# three operator interiors and the probes stub were filled — the structured validity rules
# generated everything else. Same three mechanical workspace-member deviations as
# genesis-demo/: no [workspace] table, fixture version/publish flags, cargo fmt.
[package]
name = "relay-app"
version = "0.0.0"
publish = false
edition = "2021"


[dependencies]
# The discipline under consumption: value objects, edges, discovery, `#[algebra]`.
boundary-spec = { path = ".." }

[dev-dependencies]
# The freeze/drift-gate mechanics — only `tests/freeze_gate.rs` and `examples/freeze.rs`
# touch it, so a dev-dependency suffices.
spec-lock = { path = "../spec-lock" }

[build-dependencies]
# The build-time discipline (tier partition, boundary grammar, inward rule, rats-nest,
# edge-probe completeness, qualify census), attached from this crate's own build.rs.
boundary-enforce = { path = "../boundary-enforce" }
