[package]
name = "probe-algebra"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "demo"
path = "src/main.rs"

[dev-dependencies]
proptest = "1"
trybuild = "1.0.117"

[build-dependencies]
syn = { version = "2", features = ["full", "visit"] }
