[package]
name = "fire-drill"
version = "0.1.0"
edition = "2021"
description = "Prove your gates still fire: a standing battery of known-bad fixtures every gate must reject — mutation testing for pipelines, including pure-prose ones."
license = "MIT OR Apache-2.0"
repository = "https://github.com/software-is-art/probe-algebra"
readme = "README.md"
keywords = ["gate", "validation", "vacuous", "negative-testing", "ci"]
categories = ["development-tools::testing"]

[dependencies]
