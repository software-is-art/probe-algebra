# GENERATED by genesis from the `credit-app` system declaration — the layout is DERIVED,
# not transcribed. Regenerate from the declaration rather than hand-porting structure.
#
# THE FLAGSHIP DEMO (docs/roadmap.md's "blank slate to green"): this tree was generated by
# `cargo run --example genesis -- examples/genesis_demo.rs genesis-demo --deps-path ..`,
# then ONLY its `MEANING:` holes were filled and the locks blessed — the structure is
# genesis's. Three mechanical deviations from standalone output, all to live INSIDE this
# workspace as a CI-tested member: the generated `[workspace]` table is removed (a member
# cannot be its own workspace), `publish = false`/`version = "0.0.0"` mark it a fixture,
# and `cargo fmt` was applied (rustfmt orders imports/modules alphabetically; genesis
# emits declaration order).
[package]
name = "credit-app"
version = "0.0.0"
edition = "2021"
publish = false

[dependencies]
# The discipline under consumption: value objects, edges, discovery, `#[algebra]`.
boundary-spec = { path = ".." }

[dev-dependencies]
# The freeze/drift-gate mechanics — only `tests/freeze_gate.rs` and `examples/freeze.rs`
# touch it, so a dev-dependency suffices.
spec-lock = { path = "../spec-lock" }

[build-dependencies]
# The build-time discipline (tier partition, boundary grammar, inward rule, rats-nest,
# edge-probe completeness, qualify census), attached from this crate's own build.rs.
boundary-enforce = { path = "../boundary-enforce" }
