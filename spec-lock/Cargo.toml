[package]
name = "spec-lock"
version = "0.1.1"
edition = "2021"
description = "Freeze a deterministically derived spec artifact into a committed file, and gate CI on drift — so the PR diff is the ratification."
license = "MIT OR Apache-2.0"
repository = "https://github.com/software-is-art/probe-algebra"
readme = "README.md"
keywords = ["golden", "snapshot", "drift", "ci", "spec"]
categories = ["development-tools::testing"]

[dependencies]
