//! bundle-demo — THE BUNDLE-BORN MEMBER: proof that the continuation process can carry a
//! module from nothing to a judged, locked, sensitivity-swept algebra with the CLI as the
//! only interface — no text was ever written into an open file (see MANIFEST.md for every
//! verb invocation, and docs/roadmap.md's "the bundle is the source").
//!
//! What this member proves, each half drift-gated in `tests/contract.rs`:
//!
//! * **grown additively** — every item in `tally.rs` arrived via `bundle add`, placed
//!   canonically by the placer (the round-trip pin holds the file to `Bundle::render`);
//! * **declared** — the contract (`commutative`/`associative`/`idempotent` merge,
//!   `identity(merge, floor)`) rode `bundle lift`'s declarations into the committed
//!   zero-annotation lift, and `Distance` judges it MET on every test run;
//! * **discovered and locked** — the spec and mutation locks in `spec/` freeze what the
//!   engine found (regenerate via `cargo run -p bundle-demo --example freeze`);
//! * **derived, not transcribed** — `tally_lift.rs` is `bundle lift`'s output, held to the
//!   scan byte for byte, so the lift can never drift from the module it lifts.

pub mod tally;
